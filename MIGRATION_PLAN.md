# boltzpmp 移植計画

## 目的

`boltzpm` のプロパゲータ法による電子スオーム解析を、数値的な意味を維持したまま
Rustへ移植する。低換算電場で長時間となる時間発展を高速化し、Pythonから利用できる
wheelとして配布可能にする。

## 守る仕様

- 内部計算は `f64` を使用する。
- エネルギー・角度セルの定義、境界面積、体積を旧版と一致させる。
- 移流境界フラックスは共有境界につき一度定義し、粒子数保存を維持する。
- 衝突の等方再注入、二セル間のエネルギー分配、電離時の二電子生成を維持する。
- DC/RFの時間刻み、自動ブレンディング係数探索、収束判定を互換実装する。
- 数値手法を変える収束加速は、互換実装完成後の明示的な実験機能とする。

## 構成

```text
boltzpmp/
  Cargo.toml
  pyproject.toml
  rust/
    boltzpmp-core/      # Python非依存の数値計算
    boltzpmp-py/        # PyO3バインディング
  python/boltzpmp/      # 公開Python API
  tests/
    parity/             # 旧Python版との比較
    physics/            # 保存則と物理的性質
  benchmarks/
  reference/            # 旧版から生成した固定基準値
```

## フェーズと完了条件

### 0. 旧版基準の固定

- 現行テストが成功することを確認する。
- メッシュ、移流演算子、衝突演算子、固定ステップ状態を保存する。
- DC/RFの代表結果と実行時間を保存する。
- 生成コマンド、Python/NumPy/SciPyのバージョンを記録する。

### 1. Rust/Pythonプロジェクトの構築

- Cargo workspaceを `boltzpmp-core` と `boltzpmp-py` に分離する。
- Maturin mixed projectとして `boltzpmp._core` を生成する。
- `cargo test` とPython import smoke testを通す。

### 2. 数値部品の移植

- 定数、メッシュ、断面積補間、混合気体を移植する。
- 移流は境界係数を事前計算する行列フリー演算子とする。
- 衝突はエネルギー方向の疎な再注入と角度重みへ分離する。
- 演算子単体で旧版fixtureと比較する。

### 3. DC/RFソルバーの移植

- 固定時間刻み、正規化、負値検出、収束判定を移植する。
- ξ探索、warm start、RF波形、位相差計算を移植する。
- 旧版と同じ公開引数・出力属性をPython層で提供する。

### 4. 最適化と並列化

- 時間ループ内でメモリを確保せず、作業バッファを再利用する。
- 単一計算内のRayon並列は、逐次版との比較後に有効化する。
- E/N掃引など独立計算を並列実行できるAPIを用意する。
- PythonインタープリタからdetachしてRust計算を実行する。

### 5. 配布

- Windows、Linux、macOS向けwheelとsdistをCIで構築する。
- 型スタブ、`py.typed`、README、移行ガイドを同梱する。
- TestPyPIでクリーンインストール試験後、明示承認を得てPyPIへ公開する。

## 数値検証ゲート

| 比較対象 | 初期基準 |
|---|---:|
| メッシュ配列 | `rtol=1e-13`, `atol=1e-14` |
| 演算子係数 | `rtol=1e-12`, スケール依存の `atol` |
| 100固定ステップ後の状態L1差 | `1e-10` 以下 |
| 固定ステップ物理量の相対差 | `1e-8` 以下 |
| 収束後EEDFのL1差 | `1e-5` 以下 |
| 収束後主要物理量の相対差 | `1e-5` 以下 |

並列リダクションによる差が確認された場合は、保存則を満たすことを前提に、実測根拠を
記録して許容差を調整する。

## 性能ゲート

- 同一ハードウェア、同一入力、同一ステップ数または収束条件で比較する。
- 最初の合格基準は、代表DC/RF計算で旧版の2倍以上とする。
- 単一計算内並列が4物理コアで1.5倍未満の場合、既定では逐次実行する。
- wall timeに加えて、steps/s、セル更新数/s、ξ再試行回数を記録する。

## 公開前の判断事項

- 配布名・import名は `boltzpmp` とする。
- `abi3-py310` の成立を早期検証し、難しい場合はPython版別wheelへ切り替える。
- Gitリポジトリ作成、GitHub連携、TestPyPI/PyPI公開は別途承認後に行う。

## 実装状況（2026-08-15）

| フェーズ | 状態 | 確認内容 |
|---|---|---|
| 0. 旧版基準の固定 | 完了 | 旧版26テスト、fixture、2条件の性能基準を保存 |
| 1. プロジェクト構築 | 完了 | Cargo workspace、PyO3、Maturin、abi3-py310 |
| 2. 数値部品の移植 | 完了 | メッシュ、移流、衝突を旧版fixtureと比較 |
| 3. DC/RF移植 | 完了 | 固定ステップ、DC、RFの分布・物理量比較が成功 |
| 4. 最適化と並列化 | 完了 | 逐次Rust高速化、実験的セル並列、DC掃引並列を実装 |
| 5. 配布 | 一部完了 | Windows wheel/sdistと隔離インストール成功。OS別CI・TestPyPIは未実行 |

代表54,000セル・5,000ステップでは、旧Python版3.73秒に対し逐次Rust版
1.16秒（約3.2倍）だった。単一計算内Rayon並列は1.61秒と遅くなったため既定無効。
同じ計算4件の掃引では、逐次4.54秒から4スレッド2.15秒（約2.11倍）へ短縮した。
